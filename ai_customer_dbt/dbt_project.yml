name: 'ai_customer_dbt'
version: '1.0.0'
config-version: 2

profile: 'ai_customer_dbt'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

models:
  ai_customer_dbt:
    staging:
      +materialized: view
      +schema: STAGING

    analytics:
      +materialized: table
      +schema: ANALYTICS